# face_servo_tracking
Face detection, tracking and recognition + servo-control of arduino robotic head

The version in the "Development branch" does not work anymore, and is a work-in-progress.  

The version is "main_1 branch" works (sort of).... but the performance issue of the data transfer is so important that it prevent normal functionability. So no it does not work. All the structure of the program had to be rethought (see "main_2 branch" for a the last version without bugs and "working", but not finished and not functional)

--------
To DO: 
Tornado server to display the video and the face data.
